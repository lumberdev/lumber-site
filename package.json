{
  "name": "lumber",
  "description": "description",
  "version": "0.0.1-alpha",
  "scripts": {
    "deps": "yarn install",
    "watch": "shadow-cljs -A:dev/cljs watch app;",
    "release": "shadow-cljs -A:dev/cljs release app;",
    "release-dev": "shadow-cljs release app && rm -rf docs/** && cp -r resources/public/** docs/;",
    "server": "shadow-cljs server;",
    "clean": "rm -rf target; rm -rf public/js/"
  },
  "devDependencies": {
    "shadow-cljs": "^2.8.69"
  },
  "dependencies": {
    "framer-motion": "^1.6.15",
    "react": "^16.8.6",
    "react-anime": "^2.2.0",
    "react-dom": "^16.12.0",
    "rooks": "^3.5.0"
  }
}
